 
         import json
        import ast
 
                locals()['f%s' % key] = factory.BuildFactory()
                for item in self.configdict[key].iterkeys():
                    if item == "steps":
                        steplist=json.loads(json.dumps(self.configdict[key][item]))
                        print steplist
                    if item == "layers":
                        for layerkey in ast.literal_eval(self.configdict[key][item]):
                            print ast.literal_eval(self.configdict[key][item])[layerkey]
